# Solo SaaS — Cursor AI 技术栈规约
# 本文件由 AI 自动遵循，勿随意删除

## 项目定位
一人公司 SaaS 全栈模板，目标：需求入 → 代码出 → 线上跑

## 技术栈（强制遵守）
- **框架**: Next.js 15 (App Router, React Server Components)
- **语言**: TypeScript（严格模式，禁止使用 `any`）
- **样式**: Tailwind CSS v4 + shadcn/ui 组件库
- **数据库 ORM**: Prisma（Schema-First 开发）
- **数据库**: Supabase (PostgreSQL)
- **认证**: NextAuth.js v5 / Supabase Auth
- **部署**: Vercel（自动 CI/CD）
- **测试**: Playwright（E2E）+ Vitest（单元）
- **监控**: Sentry

## 代码规范
1. 所有组件使用 **函数式组件**，禁止 class component
2. Server Component 优先，Client Component 加 `"use client"` 标注
3. 数据获取在 Server Component 中进行，避免瀑布请求
4. API 路由统一放 `src/app/api/` 下，返回标准 `{ data, error }` 结构
5. 数据库操作统一通过 `src/lib/db.ts` 的 Prisma Client 单例
6. 环境变量通过 `src/env.ts`（zod 校验）访问，禁止直接 `process.env`
7. 错误处理：Server Action 返回 `{ success, error }`，Client 侧用 `toast` 展示

## 目录结构约定
```
src/
  app/           # Next.js App Router 页面
    (auth)/      # 认证相关路由组
    (dashboard)/ # 登录后主界面路由组
    api/         # API Routes
  components/
    ui/          # shadcn/ui 基础组件（不要手动修改）
    features/    # 业务功能组件
    layouts/     # 布局组件
  lib/
    db.ts        # Prisma Client 单例
    auth.ts      # NextAuth 配置
    utils.ts     # 通用工具函数
  types/         # 全局 TypeScript 类型定义
  env.ts         # 类型安全的环境变量
prisma/
  schema.prisma  # 数据库模型（唯一数据来源）
```

## AI 生成代码时必须遵守
- 生成组件时，同时生成对应 Storybook / Playwright 测试占位
- 新建 API Route 必须加入 `api.http` 的对应测试用例
- 数据库字段变更必须先更新 `schema.prisma`，再生成迁移
- 所有用户输入必须用 Zod 校验，防止注入
- 不要在客户端暴露数据库 ID，使用 CUID2 或 UUID

## 性能红线
- 首屏 LCP < 2.5s，CLS < 0.1
- API 响应 P95 < 500ms
- Bundle Size: 首屏 JS < 100kb gzip
